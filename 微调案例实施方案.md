# 汽车售后客服助手：SFT + DPO 端到端微调案例实施方案

> 文档状态：已与需求方讨论确认，待开始执行
> 对应需求：`需求.txt`
> 创建日期：2026-08-19

---

## 1. 项目目标

做一个**端到端真实的 LLM 微调案例**，完整覆盖：**数据构造 → 微调训练 → 效果评估**，分两个阶段：

| 阶段 | 内容 | 成功标准 |
|---|---|---|
| 第一阶段 SFT | 场景选定 → 数据类别拆解 → LLM 构造数据 → **LoRA SFT** | evalscope + LLM-as-a-Judge 评估：**微调后效果 > 纯 Prompt Engineering 基线** |
| 第二阶段 DPO | 构造偏好对（chosen/rejected）→ **LoRA DPO**（在 SFT 模型基础上） | evalscope + LLM-as-a-Judge 评估：**DPO 后更符合人类偏好 > SFT 模型** |

要求：场景真实有价值、数据可复现构造、每一步评估都有量化对比、结论可验证。

---

## 2. 场景选择：汽车售后 / 4S 店客服助手

### 2.1 为什么选这个场景（真实价值）

- 中国汽车保有量超 3 亿辆，售后是高频、高价值、强流程化的场景
- 车主问题高度重复（保养周期、质保范围、故障灯、价格、投诉）、答案高度标准化
- 这是微调最擅长的领域：**同样的知识，用统一的格式、统一的语气、稳定地答出来**
- 痛点真实：人工客服成本高、响应慢、话术不统一、知识库更新难

### 2.2 真实案例背书（2025 年中至今）

| 案例 | 来源 | 说明 |
|---|---|---|
| Impel 微调 LLM 服务汽车经销商客户互动 | AWS 官方博客（2025.6） | "微调 + 汽车客服"标杆案例 |
| 长安汽车"智能小安"AI 客服 | 京东 AI 赋能（2025.6） | 客服体验分跃升 78.9% |
| 丰田 × 中关村科金大模型语音智能体 | 2025 | 老客运营触达接通率 60%，售后回访智能化 |
| 4S 店 AI 语音数字员工售后回访 | 2025 | 回访效率提升 400% |
| 一汽丰田专家级汽车服务智能客服 | 2025 | "AI 驱动安全护航"（故障分诊类功能） |
| 现代汽车 × Google Cloud AI 技术助理 | 2025 | 症状描述 → 初步判断 + 下一步建议 |
| 腾讯云 ADP 智能体 | 2025 | 故障排查 + 维修店推荐 |
| 4S 店本地 AI 生成客户关怀话术与投诉应对预案 | 2025 | 话术生成直接落地 4S 店 |

### 2.3 使用者形态（真实企业的混合模式）

```
车主消息进来
   │
   ▼
AI 判断：这单我能搞定吗？
   ├─ 高置信度（保养咨询/约期调整等）→ AI 直接回车主（车主直连形态）
   └─ 低置信度/投诉/要赔偿 → 转人工 + AI 给员工生成回复草稿（坐席辅助形态）
```

- **形态 A（车主直连）**：输入 = 车主口语；对应数据类别 1、2
- **形态 B（坐席辅助）**：输入 = 场景描述 + 客户原话；对应数据类别 3、4

本案例数据构造按两种输入形态各覆盖一部分，复刻真实产品形态。

---

## 3. 数据类别拆解（4 类）

> **设计决策**：真实系统中"意图识别"是独立的 NLU 节点（小模型/分类器，负责路由），不属于对话生成模型职责。因此本案例**不设意图识别类别**，聚焦"回复生成"本身。方案文档第 8 节专门说明真实系统如何与之配合。

### 3.1 类别一：售后知识问答（答得准）

- **任务**：准确回答保养周期、质保政策、配件价格、保险理赔流程等
- **业务价值**：占客服咨询量 60%+，知识重复、答案固定
- **格式要求**：准确引用政策、给出可执行信息、可反问是否预约
- **示例**：
```
输入：我去年3月买的车，现在1万3千公里，首保免费吗？
输出：首保免费条件是购车6个月或5000公里内（先到为准），您已超期需自费约400-600元。
     之后每10000公里或12个月保养一次（先到为准）。需要我帮您预约吗？
```

### 3.2 类别二：故障诊断与安全分级（分得险）

- **任务**：根据车主症状描述，输出可能原因 + 紧急程度（安全分级）+ 保守可执行的建议
- **业务价值**：售后最有技术含量的一环；安全红线，真实企业主打"安全护航"
- **格式要求**：必须含【可能原因】【紧急程度】【建议】三要素，保守优先
- **安全分级表**：
  | 紧急程度 | 典型症状 | 建议动作 |
  |---|---|---|
  | 🔴 高（立即停车） | 刹车失效/变软、水温爆表、冒浓烟、方向失控、机油灯+异响 | 靠边停车、呼叫拖车、禁止行驶 |
  | 🟡 中（尽快进店） | 发动机故障灯、漏油、抖动加剧、异响持续 | 可短途开至门店，尽快检测 |
  | 🟢 低（可预约） | 轻微异响、雨刮老化、胎压低、保养到期 | 正常预约，常规检查 |
- **示例**：
```
输入：刹车踩下去感觉软绵绵的，制动力下降。
输出：【可能原因】制动液不足或制动系统漏油
       【紧急程度】高——立即停车，呼叫拖车，禁止继续行驶
       【建议】安排拖车到店检测，技师检查制动液位和管路
```

### 3.3 类别三：服务话术生成（说得好）

- **任务**：给定"为难的业务场景"（约满/涨价/缺货/延误/催进度），生成礼貌、专业、可执行的回复
- **与普通问答的区别**：问答是"答对问题"，话术是"把话说好"
- **话术标准骨架（企业规范）**：
  ```
  话术 = 缓冲/共情（先接住情绪）
       + 事实说明（客观情况）
       + 给出路（替代方案/补救）
       + 促行动（预约/到店）
  ```
- **示例**：
```
输入：场景：客户要求周末保养，但周末工位已满，最快下周三上午。
输出：非常抱歉让您久等了！本周末保养工位确实已全部约满，最快可安排到下周三上午 9:00。
     您看这个时间方便吗？如果不方便，我也可以帮您协调邻近门店，
     或者先帮您锁定下周三的工位，您再考虑一下？
```

### 3.4 类别四：投诉处理与安抚（灭得火）

- **任务**：车主已愤怒（要赔偿/要说法），客服要接住情绪、界定责任、给补救、留升级路径
- **与话术生成的差别**：情绪浓度更高，重"先灭火再解决"
- **投诉处理标准骨架**：
  ```
  投诉回复 = 共情承认（先接住情绪，不辩解）
           + 责任界定（是我们的问题就认，不推诿）
           + 补救方案（具体、可执行、有诚意）
           + 升级路径（给客户"不满意还有更高渠道"的安全感）
  ```
- **示例**：
```
输入：客户原话：你们保养完我的车发动机噪音变大了，肯定没换好机油！我要投诉，必须给我个说法！
输出：非常理解您的心情，保养后出现这样的问题确实让人不放心，我先代表门店向您道歉。
     我们立即安排免费复检，由技术总监亲自检查机油加注记录和发动机状态。
     如果确认是我们的责任，全额退还本次保养费，并额外补偿一次免费保养。
     如果您对处理结果仍有疑问，可以拨打 400 服务监督热线，我们全程跟进。
```

### 3.5 各类别与 SFT / DPO 的关系

> **重要澄清：4 类数据全部参与 SFT，也全部参与 DPO。** DPO 的前提是"先会答，再答得好"，没有 SFT 打底，模型连基本骨架都组织不出来，DPO 无从优化。

| 类别 | SFT 提升重点 | DPO 提升重点 |
|---|---|---|
| 1 知识问答 | 知识内化 + 格式稳定（提升最明显） | 语气更友好、主动促行动 |
| 2 故障诊断 | 领域知识 + 安全分级（提升最明显） | 表达更保守稳妥 |
| 3 话术生成 | 学会标准骨架 | 共情与"给出路"（区分度强） |
| 4 投诉处理 | 学会标准骨架 | 共情/担责/补救（区分度最强） |

- 前两类（1、2）：**SFT 前后分差肉眼可见**（知识记住了、格式稳了）
- 后两类（3、4）：**DPO 前后分差拉得开**（既会答又懂人情）

---

## 4. 数据格式与规模

### 4.1 SFT 数据格式（trl 标准 messages，每行一条 JSON）

```jsonl
{"messages": [{"role": "user", "content": "我去年3月买的车，现在1万3千公里，首保免费吗？"}, {"role": "assistant", "content": "首保免费条件是购车6个月或5000公里内（先到为准），您已超期需自费约400-600元。之后每10000公里或12个月保养一次。需要我帮您预约吗？"}]}
```

### 4.2 DPO 数据格式（prompt + chosen + rejected，每行一条 JSON）

```jsonl
{"prompt": "客户反映上次保养后发动机噪音变大，怀疑店里没换好机油，情绪激动要求赔偿。请处理投诉。", "chosen": "非常理解您的心情，保养后出现问题确实让人不放心……若确认是我们的责任，将全额退还本次保养费并补偿一次免费保养。", "rejected": "发动机噪音变大有很多原因，不一定是我们保养的问题。建议您先开过来检查，检查费 200 元，如果是我们的问题再谈赔偿。"}
```

### 4.3 评估数据格式（evalscope general_qa，每行一条 JSON）

```jsonl
{"query": "我去年3月买的车，现在1万3千公里，首保是不是免费的？多久保养一次？"}
```

### 4.4 数据规模

| 数据集 | 规模 | 说明 |
|---|---|---|
| SFT 训练集 | ~2000 条（4 类各 ~500 条） | 用于 LoRA SFT |
| SFT 测试集 | ~200 条（4 类各 ~50 条） | 单独留出，**不参与训练**，用于评估 |
| DPO 偏好对 | ~1000 对（话术生成 + 投诉处理为主，约 600 对） | 用于 LoRA DPO |
| 评估集 | = SFT 测试集 ~200 条 | 同一批 query 喂给基座/SFT/DPO 三个模型对比 |

> 说明：SFT 数据按 9:1 划分训练/测试；测试集不参与任何训练，保证评估公平。

---

## 5. 数据构造方法

### 5.1 总体流程

```
① 种子场景清单（人工整理，真实车主高频问题）
   → ② LLM 批量生成（DeepSeek API）
   → ③ 规则校验（格式/必含要素/长度）
   → ④ 人工抽检清洗（10%~20%）
   → ⑤ 划分 train/test
```

### 5.2 各类别构造要点

- **类别 1（知识问答）**：先整理"政策/价格知识表"（保养周期、质保条款、常用配件价格区间等），LLM 据此生成问答对，保证答案事实正确
- **类别 2（故障诊断）**：**知识规则表兜底**——先做"症状→可能原因→紧急程度"规则表（30~50 条，来自维修手册/正规科普），LLM 只负责把症状扩写成多样化的车主口语描述，回复按规则生成。**禁止 LLM 自由发挥修车建议**
- **类别 3（话术生成）**：枚举真实为难场景（约满/涨价/缺货/延误/催进度），按"缓冲→事实→出路→促行动"骨架生成
- **类别 4（投诉处理）**：枚举投诉类型（保养质量问题/收费争议/维修延误/态度问题），按"共情→担责→补救→升级"骨架生成

### 5.3 DPO 偏好对构造（两种方式、两个阶段，共 ~1000 对）

**DPO 数据格式**（trl 标准）：`{"prompt": "...", "chosen": "...", "rejected": "..."}`，prompt 复用 SFT 数据的用户输入（保证 SFT/DPO 场景一致、评估对比公平）。

**偏好对分布**（后两类为主，共 ~1000 对）：

| 类别 | 偏好对 | rejected 的主要缺陷维度 |
|---|---|---|
| 1 知识问答 | ~200 对 | 漏关键信息 / 价格乱报 / 语气冷淡 / 不回应诉求 |
| 2 故障诊断 | ~200 对 | **紧急程度误判**（高判低，如"没事，您慢慢开过来"）/ 建议不安全 / 无到店检测建议 |
| 3 话术生成 | ~300 对 | 不道歉 / 只拒绝不给方案 / 语气生硬 / 不促行动 |
| 4 投诉处理 | ~300 对 | 辩解推诿 / 无共情 / 无补救 / 无升级路径 |

**阶段 A（SFT 数据就绪即可做，占 ~70%）——LLM 刻意改写坏回复**：
- 复用 SFT 数据的 `user` 输入当 prompt、标准回复当 chosen
- 用 LLM 按上表"缺陷维度"把好回复刻意改写成差回复（如投诉处理：开头辩解推诿、不给补救、无升级路径）
- 不依赖 SFT 模型，可与 SFT 训练并行

**阶段 B（SFT 训练完成后做，占 ~30%）——模型生成 + LLM 排序**：
- 用训练好的 SFT 模型对同一批 prompt 生成多条回复 → DeepSeek 裁判排序 → 最高分做 chosen、最低分做 rejected
- 依赖 SFT 模型，体现"真实模型输出"的偏好差异，最有说服力

**规则校验**：rejected 必须含可判定的缺陷（关键词/结构检查 + 人工抽检），防止"两条都好"或"两条都差"导致 DPO 学不到东西。

### 5.4 质量红线

- 故障诊断：紧急程度分级必须保守（宁可让车主多跑一趟，不能让他带故障行驶）
- 投诉处理：不得推诿、不得过度承诺赔偿金额
- 政策/价格：标注"以门店实际为准"的兜底表述

---

## 6. 训练方案

### 6.1 基座模型选择

| 方案 | 模型 | 适用 | 说明 |
|---|---|---|---|
| **主方案（云上）** | **Qwen3-8B** | 最终案例 | 云上 GPU（如 AutoDL RTX 4090 24G），LoRA 训练，效果上限高、说服力强 |
| 快速验证（本地） | Qwen3-0.6B（instruct 版，本地已有） | 流程冒烟测试 | 16GB 显存绰绰有余，一个 epoch 几分钟，用于验证脚本与流程 |

> 用 instruct 版做基线对比（公平）：Prompt Engineering 基线基于已经会聊天的 instruct 模型，SFT 后还要比它更好，这才是真实效果。本地 `model/Qwen3-0.6B` 为 instruct 版，`Qwen3-0.6B-Base` 为 base 版（基线不公平，不用）。

### 6.2 SFT（LoRA）

- 框架：`trl` SFTTrainer / `transformers` Trainer + `peft`
- LoRA 配置建议（8B）：`r=16, alpha=32, dropout=0.05`，`target_modules` 覆盖 q/k/v/o/gate/up/down 全线性层
- 训练参数建议：epochs 2~3；lr 2e-4；有效 batch size 32（micro 4 × grad accum 8）；max_seq_len 1024~2048；BF16；adamw
- 输出：LoRA adapter → 合并回基座，产出 **SFT 模型**

### 6.3 DPO（LoRA）

- 框架：`trl` DPOTrainer（在 SFT 模型上继续 LoRA）
- LoRA 配置与 SFT 一致
- 训练参数建议：epochs 1~2；**lr 1e-5~5e-6（DPO 学习率明显更低）**；beta 0.1
- 输出：LoRA adapter → 合并回 SFT 模型，产出 **DPO 模型**

### 6.4 训练注意事项

- 显存不足时：4bit QLoRA（bitsandbytes）降显存
- 合并模型用 `peft` 的 `merge_and_unload`，产出标准模型目录供 evalscope 加载

---

## 7. 评估方案

### 7.1 evalscope + LLM-as-a-Judge

- 评估框架：**evalscope**（当前未安装，执行前需安装）
- 裁判：**DeepSeek API（LLM-as-a-Judge）**，配置参考现有 `11_eval_command.sh`：
  - `judge-strategy: llm`，`score_type: numeric`，`score_pattern: [[0~1 小数]]`
  - 裁判生成温度 0.0，保证稳定可复现
- 数据格式：evalscope `general_qa`（`benchmark/custom_eval/text/qa/` 下按类别建 subset）

### 7.2 评估维度（judge 打分模板）

| 类别 | 维度（权重） |
|---|---|
| 1 知识问答 | 格式规范（0.3）、信息准确（0.4）、语气友好（0.2）、方案可执行（0.1） |
| 2 故障诊断 | **紧急程度判断正确（0.5，安全项，错判直接低分）**、建议保守可执行（0.3）、格式完整（0.2） |
| 3 话术生成 | 骨架完整（0.3）、共情与给出路（0.4）、语气（0.2）、促行动（0.1） |
| 4 投诉处理 | 共情承认不推诿（0.4）、责任界定与补救（0.4）、升级路径（0.1）、语气（0.1） |

> 按类别分 subset 评估，可得到**分类别分数**和**总分**两张对比表。

### 7.3 对比基线（同一测试集 ~200 条 query）

| 对比组 | 模型 | 结论要求 |
|---|---|---|
| 基线 | 基座模型 + 精心设计的 Prompt Engineering | — |
| 第一组 | **SFT 模型 vs 基线** | SFT 均分 > 基线均分（SFT 有效） |
| 第二组 | **DPO 模型 vs SFT 模型** | DPO 均分 > SFT 均分，且"共情/语气/方案"维度优势明显（DPO 偏好对齐有效） |

- 三个模型用**同一批 query、同一个 judge 配置、温度 0**，保证对比公平
- 结果取多次评估平均值，避免单次波动

### 7.4 成功标准

1. SFT 模型在测试集上的 judge 均分 **明显高于** Prompt Engineering 基线（重点看格式与知识维度）
2. DPO 模型 judge 均分 **高于** SFT 模型，尤其在投诉处理/话术生成类别的"共情、语气、方案"维度
3. 所有结论附上分类别分数表 + 典型案例对比（同一 query 三个模型的输出并排展示）

---

## 8. 与真实系统的关系（教学说明）

### 8.1 意图识别为什么不在本案例

真实客服系统中，意图识别是**独立的 NLU 节点**（通常是小模型/分类器 + 槽位抽取），负责"路由"，与"回复生成"解耦：

```
用户消息 → [NLU 节点：意图+槽位+置信度] → [对话管理：决定动作]
         → [知识库/预约系统/转人工] → [回复生成（本案例微调的模型）]
```

理由：成本（分类用小模型即可）、延迟（毫秒级先行响应）、可观测（低置信度自动转人工）、可单独迭代。本案例聚焦"回复生成"模型微调；如需意图识别，可作扩展练习（用 `Qwen3-0.6B-Base` 单独训一个意图分类器）。

### 8.2 知识过期边界

微调内化的政策/价格有保质期，真实落地需配合知识库外挂（RAG），微调负责"怎么答得好"。作为教学案例，此边界不影响结论。

---

## 9. 运行环境与基础设施

| 项 | 配置 |
|---|---|
| 云 GPU（训练） | AutoDL 或阿里云 PAI-DSW，RTX 4090 24G（约 ¥1.5~2.5/时）；预算足可 A100 |
| 本地（数据构造/评估脚本开发） | 当前项目环境：RTX 5070 Ti Laptop 16GB |
| 训练栈 | transformers 5.2.0 / trl 0.24.0 / peft 0.18.1 / unsloth / bitsandbytes / accelerate（本地 venv 已有） |
| **evalscope** | **未安装，执行评估前安装** |
| 裁判 API | DeepSeek API（需 `DEEPSEEK_API_KEY`） |
| 数据构造 LLM | DeepSeek API 批量生成（不占 GPU） |

分工：数据构造本地做（不占 GPU）；训练云上 GPU 跑；评估推理可在云上 serve 或本地跑；裁判用 DeepSeek API。

---

## 10. 实施步骤与里程碑

| # | 里程碑 | 产出 | 状态 |
|---|---|---|---|
| 1 | 方案确认 | 本文档 | ✅ 已完成 |
| 2 | 环境准备 | 云上实例就绪、evalscope 安装、DeepSeek API Key 确认 | 🔄 进行中（DEEPSEEK_API_KEY 待配置，见操作日志 LOG-003） |
| 3 | 种子场景清单 | 4 类种子清单各 25 条（共 100 条）+ README，见 `data/seeds/` | ✅ 已完成（2026-08-19） |
| 4 | 小批样本验货 | 每类生成 10 条样本，人工确认数据质量（共 40 条，已确认） | ✅ 已完成（2026-08-19） |
| 5 | 批量数据构造 | SFT 训练集 2041 条 + 测试集 199 条（共 2240，按种子分组切分），评估集 199 条已生成 | ✅ 已完成（2026-08-19，投诉处理加量至 742 条） |
| 6 | SFT 训练 | LoRA SFT 全量训练完成（本地 Qwen3-0.6B，2041 条×3 epochs，eval_loss 1.959）→ 已合并 `output/sft_model_merged/` | ✅ 已完成（2026-08-20，见操作日志 LOG-016；云上 8B 留作可选增强） |
| 7 | 第一轮评估 | 基线（Prompt Engineering）vs SFT，evalscope + judge，出结论 1 | ✅ 已完成（2026-08-20，SFT 0.610 vs 基线 0.249，全面超越，见操作日志 LOG-021） |
| 8 | DPO 数据构造 | 偏好对 998 对（阶段 A 改写型完成：200/200/300/300）；阶段 B 模型排序型可选待定 | 🔄 阶段 A ✅（2026-08-20，见 LOG-023）；阶段 B 可选 |
| 9 | DPO 训练 | LoRA DPO 完成（998 对 × 2 epochs，accuracies 0.99，margins 2.77）→ 已合并 `output/dpo_model_merged/` | ✅ 已完成（2026-08-20，见 LOG-025） |
| 10 | 第二轮评估 | SFT vs DPO，出结论 2（DPO 0.631 > SFT 0.610，偏好对齐有效） | ✅ 已完成（2026-08-21，见 LOG-026） |
| 11 | 总结报告 | 完整案例报告：数据/训练/评估全过程 + 分类别分数表 + 典型案例对比 | ⬜ |

> **同步机制**：每个里程碑完成后，更新上表状态（⬜→✅），并在 `操作日志.md`（append-only）追加记录。日志约定见该文件头部。

---

## 11. 风险与注意点

| 风险 | 应对 |
|---|---|
| LLM 构造数据质量不稳 | 规则校验（格式/要素）+ 人工抽检 10%~20% |
| 故障诊断知识错误（安全风险） | 知识规则表兜底，LLM 只扩写表述，禁止自由发挥 |
| 投诉处理过度承诺 | 构造数据时统一"以门店确认为准"表述，抽检重点看 |
| judge 打分波动 | 温度 0 + 同一配置 + 多次取平均 |
| 0.6B 效果天花板 | 主方案云上 8B；0.6B 仅用于流程冒烟验证 |
| 政策/价格知识过期 | 教学案例边界：真实落地需 RAG 外挂；文档中说明 |

---

## 12. 目录结构规划（建议）

```
fine_tune_proj/
├── 需求.txt                    # 原始需求
├── 微调案例实施方案.md          # 本文档
├── data/
│   ├── sft_train.jsonl         # SFT 训练集 ~2000 条
│   ├── sft_test.jsonl          # SFT 测试集 ~200 条（= 评估集）
│   ├── dpo_pairs.jsonl         # DPO 偏好对 ~1000 条
│   └── seeds/                  # 种子场景清单（4 类）
├── scripts/
│   ├── build_data.py           # LLM 批量数据构造
│   ├── validate_data.py        # 规则校验 + 抽检
│   ├── train_sft.py            # LoRA SFT
│   ├── train_dpo.py            # LoRA DPO
│   ├── merge_model.py          # 合并 LoRA adapter
│   └── eval_runner.py/sh       # evalscope 评估封装
├── benchmark/custom_eval/      # evalscope 评估数据集（按类别 subset）
├── output/
│   ├── sft_model/              # 合并后的 SFT 模型
│   ├── dpo_model/              # 合并后的 DPO 模型
│   └── eval_results/           # 评估分数与对比
└── 案例总结报告.md             # 最终报告（里程碑 11）
```

---

## 附：核心决策记录（讨论结论）

1. **场景**：汽车售后/4S 客服助手（2025 年中至今有真实落地案例背书）
2. **使用者形态**：车主直连 + 坐席辅助混合（类别 1/2 用车主口语输入，类别 3/4 用场景描述输入）
3. **去掉意图识别**：真实系统中是独立 NLU 节点，不属于生成模型职责；保留为扩展练习
4. **4 类数据全部参与 SFT 和 DPO**：SFT 打基础（会答），DPO 做偏好（答得好）
5. **故障诊断知识正确性由规则表兜底**，LLM 只做表述多样化
6. **基座模型**：主方案云上 Qwen3-8B，本地 Qwen3-0.6B 仅冒烟验证
7. **评估**：evalscope + DeepSeek LLM-as-a-Judge，同一测试集、同一配置、温度 0
8. **成功标准**：SFT > Prompt Engineering 基线；DPO > SFT（偏好维度）
