# **2、DPO的损失函数**

DPO用来训练模型在一对回答中更偏向好回答。对于同一个问题，数据中通常包含一个好回答和一个差回答。DPO不是直接训练奖励模型，而是直接利用这两个回答的对数概率差来构造损失函数。

先定义当前模型下的对数概率差：

$$
\Delta_{\theta}
= \log \pi_{\theta}(y_w \mid x)
- \log \pi_{\theta}(y_l \mid x)
$$

其中，符号`y_w`表示好回答，符号`y_l`表示差回答。

再定义参考模型下的对数概率差：

$$
\Delta_{\mathrm{ref}}
= \log \pi_{\mathrm{ref}}(y_w \mid x)
- \log \pi_{\mathrm{ref}}(y_l \mid x)
$$

DPO真正优化的是当前模型相对于参考模型的偏好提升：

$$
\delta
= \Delta_{\theta} - \Delta_{\mathrm{ref}}
$$

展开就是：

$$
\delta
=
\left[
\log \pi_{\theta}(y_w \mid x)
- \log \pi_{\theta}(y_l \mid x)
\right]
-
\left[
\log \pi_{\mathrm{ref}}(y_w \mid x)
- \log \pi_{\mathrm{ref}}(y_l \mid x)
\right]
$$

DPO的损失函数为：

$$
L
=
-\log \sigma(\beta \delta)
$$

其中：

$$
\sigma(t)
=
\frac{1}{1 + e^{-t}}
$$

也就是说，DPO先用相对偏好差表示当前模型相对于参考模型是否更偏向好回答，再用β控制偏好强度，最后用负对数形式构造损失。

## **2.1 为什么不直接优化相对偏好，对数概率差**

如果直接优化相对偏好，也就是直接让相对偏好差越大越好，那么可以把损失写成：

$$
L_0
=
-\delta
$$

对相对偏好差求导：

$$
\frac{\partial L_0}{\partial \delta}
=
-1
$$

这个梯度永远是负一。也就是说，无论模型现在错得多严重，还是已经明显偏向好回答，它都会继续用同样的力度去增大相对偏好差。

例如，假设当前模型已经非常偏向好回答：

$$
\delta = 8
$$

如果使用直接优化相对偏好的方式：

$$
\frac{\partial L_0}{\partial \delta}
=
-1
$$

梯度仍然不会变小。模型会继续被强行推动，让好回答和差回答之间的差距越来越大。

这会带来两个问题。

第一，目标没有上限。只要继续训练，模型就会继续拉大好回答和差回答的对数概率差，容易过度偏离参考模型。

第二，训练信号没有“够了”的概念。偏好数据通常只说明好回答比差回答更好，并不意味着模型必须把好回答的概率推到极端高、把差回答的概率推到极端低。

所以，DPO不直接最大化相对偏好差，而是先把相对偏好差放入sigmoid中：

$$
z
=
\sigma(\beta \delta)
$$

这样可以把任意大小的相对偏好差映射成零到一之间的偏好概率。

当相对偏好差很小时，说明模型还没有学会偏好，训练应该继续推动。

当相对偏好差很大时，说明模型已经明显偏向好回答，训练力度应该自动减弱。

## **2.2 为什么需要在sigmoid前面叫上log**

先看只使用sigmoid、不加log的情况。假设损失函数写成：

$$
L_1
=
-\sigma(\beta \delta)
$$

令：

$$
z
=
\sigma(\beta \delta)
$$

那么：

$$
L_1
=
-z
$$

对相对偏好差求导：

$$
\frac{\partial L_1}{\partial \delta}
=
-
\frac{\partial z}{\partial \delta}
$$

sigmoid的导数为：

$$
\frac{\partial z}{\partial \delta}
=
z(1-z)\beta
$$

所以：

$$
\frac{\partial L_1}{\partial \delta}
=
-\beta z(1-z) = -\beta \cdot \sigma(-\beta \cdot \delta)
$$

这个形式的问题是：当模型严重判断错误时，偏好概率会非常接近零，梯度反而会非常小。

带入具体数值：

$$
z = 0.01
$$

$$
\beta = 0.1
$$

则：

$$
\frac{\partial L_1}{\partial \delta}
=
-0.1 \times 0.01 \times (1 - 0.01)
$$

$$
\frac{\partial L_1}{\partial \delta}
=
-0.00099
$$

此时模型已经严重判断错误，但梯度绝对值只有`0.00099`，几乎推不动。

再看DPO真正使用的形式：

$$
L_2
=
-\log \sigma(\beta \delta)
$$

仍然令：

$$
z
=
\sigma(\beta \delta)
$$

则：

$$
L_2
=
-\log z
$$

对相对偏好差求导：

$$
\frac{\partial L_2}{\partial \delta}
=
-
\frac{1}{z}
\cdot
\frac{\partial z}{\partial \delta}
$$

带入sigmoid导数：

$$
\frac{\partial L_2}{\partial \delta}
=
-
\frac{1}{z}
\cdot
z(1-z)\beta
$$

约掉中间变量：

$$
\frac{\partial L_2}{\partial \delta}
=
-\beta(1-z)
$$

因为：

$$
1 - z
=
1 - \sigma(\beta \delta)
$$

而：

$$
1 - \sigma(\beta \delta)
=
\sigma(-\beta \delta)
$$

所以：

$$
\frac{\partial L_2}{\partial \delta}
=
-\beta \sigma(-\beta \delta)
$$

也就是完整推导链条：

$$
\frac{\partial L_2}{\partial \delta}
=
-
\frac{1}{z}
\cdot
z(1-z)
\cdot
\beta
$$

$$
\frac{\partial L_2}{\partial \delta}
=
-\beta(1-z)
$$

$$
\frac{\partial L_2}{\partial \delta}
=
-\beta \sigma(-\beta \delta)
$$

继续带入刚才的数值：

$$
z = 0.01
$$

$$
\beta = 0.1
$$

则：

$$
\frac{\partial L_2}{\partial \delta}
=
-0.1 \times (1 - 0.01)
$$

$$
\frac{\partial L_2}{\partial \delta}
=
-0.099
$$

对比一下：

$$
\left|
-0.099
\right|
\div
\left|
-0.00099
\right|
=
100
$$

加上log之后，在模型严重判断错误时，梯度变成了原来的`100`倍。

这就是为什么不能只用sigmoid，而要使用负对数sigmoid形式。log的作用是：当模型把好回答判断得很差时，给出足够大的惩罚和足够强的梯度。

再把梯度拆到好回答和差回答上。

因为：

$$
\delta
=
\log \pi_{\theta}(y_w \mid x)
-
\log \pi_{\theta}(y_l \mid x)
-
\Delta_{\mathrm{ref}}
$$

参考模型不参与训练，所以参考模型下的对数概率差不需要更新。

对好回答的对数概率求导：

$$
\frac{\partial \delta}
{\partial \log \pi_{\theta}(y_w \mid x)}
=
1
$$

因此：

$$
\frac{\partial L_2}
{\partial \log \pi_{\theta}(y_w \mid x)}
=
-\beta \sigma(-\beta \delta)
$$

好回答对应的梯度是负数。梯度下降时，参数会沿着负梯度方向更新，所以好回答的对数概率会被提高。

对差回答的对数概率求导：

$$
\frac{\partial \delta}
{\partial \log \pi_{\theta}(y_l \mid x)}
=
-1
$$

因此：

$$
\frac{\partial L_2}
{\partial \log \pi_{\theta}(y_l \mid x)}
=
\beta \sigma(-\beta \delta)
$$

差回答对应的梯度是正数。梯度下降时，差回答的对数概率会被降低。

所以DPO的梯度方向非常清楚：

$$
\text{提高好回答的对数概率}
$$

$$
\text{降低差回答的对数概率}
$$

## **2.3 超参数β的作用**

β出现在sigmoid内部：

$$
L
=
-\log \sigma(\beta \delta)
$$

也就是说：

$$
\beta \delta
$$

才是真正送入sigmoid的值。

β的第一个作用，是控制偏好差异被放大的程度。

假设：

$$
\delta = 3
$$

当：

$$
\beta = 0.1
$$

则：

$$
\beta\delta = 0.3
$$

$$
\sigma(0.3) \approx 0.5744
$$

$$
L = -\log(0.5744) \approx 0.5544
$$

当：

$$
\beta = 1
$$

则：

$$
\beta\delta = 3
$$

$$
\sigma(3) \approx 0.9526
$$

$$
L = -\log(0.9526) \approx 0.0486
$$

可以看到，相同的相对偏好差，β越大，进入sigmoid之前的值越大，损失越小，模型越容易认为当前偏好已经足够明显。

β的第二个作用，是影响梯度大小。

DPO对相对偏好差的梯度为：

$$
\frac{\partial L}{\partial \delta}
=
-\beta \sigma(-\beta\delta)
$$

如果：

$$
\delta = 0.3
$$

当：

$$
\beta = 0.1
$$

则：

$$
\beta\delta = 0.03
$$

$$
\sigma(-0.03) \approx 0.4925
$$

$$
\frac{\partial L}{\partial \delta}
=
-0.1 \times 0.4925
=
-0.04925
$$

当：

$$
\beta = 1
$$

则：

$$
\beta\delta = 0.3
$$

$$
\sigma(-0.3) \approx 0.4256
$$

$$
\frac{\partial L}{\partial \delta}
=
-1 \times 0.4256
=
-0.4256
$$

在相对偏好差不大的时候，β越大，梯度绝对值通常越大，模型更新越激进。

但是β不是越大越好。假设：

$$
\delta = 8
$$

当：

$$
\beta = 0.1
$$

则：

$$
\beta\delta = 0.8
$$

$$
\sigma(-0.8) \approx 0.3100
$$

$$
\left|
\frac{\partial L}{\partial \delta}
\right|
=
0.1 \times 0.3100
=
0.03100
$$

当：

$$
\beta = 1
$$

则：

$$
\beta\delta = 8
$$

$$
\sigma(-8) \approx 0.0003
$$

$$
\left|
\frac{\partial L}{\partial \delta}
\right|
=
1 \times 0.0003
=
0.0003
$$

这说明，当相对偏好差已经很大时，β太大反而会让sigmoid过快饱和，导致梯度迅速变小。

所以，β可以理解成偏好学习强度的旋钮：

- β较小：训练更保守，更不容易偏离参考模型，但学习偏好可能较慢。
- β较大：训练更激进，更快拉开好回答和差回答，但可能更不稳定。
- β过大：sigmoid容易饱和，部分样本梯度会过早变小。
- β过小：偏好信号太弱，模型可能学不明显。

总结起来：

$$
\beta
\text{控制的是偏好差异进入sigmoid前的缩放强度}
$$

它同时影响：

$$
\text{损失大小}
$$

$$
\text{梯度大小}
$$

$$
\text{模型偏离参考模型的程度}
$$
