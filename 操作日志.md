# 操作日志（Append-Only）

> **使用约定（重要）：**
> 1. 本文件**只追加、不修改**：新记录一律追加到末尾，已写条目不得改动。
> 2. 如需更正已有记录，**追加一条「更正」条目**，注明被更正记录的编号（如 LOG-003）。
> 3. 记录范围：重大决策、里程碑完成、异常与处理、环境变更、外部依赖变化等。
> 4. 编号规则：LOG-NNN，按追加顺序递增，永不复用。
> 5. 每个里程碑完成后，同步更新 `微调案例实施方案.md` 中的里程碑表格状态。

---

## 2026-08-19

### LOG-001 [决策] 案例方案确认
- 场景：汽车售后 / 4S 店客服助手（2025 年中至今有真实落地案例背书）
- 数据：4 类（知识问答 / 故障诊断 / 话术生成 / 投诉处理），SFT ~2000 条（每类 ~500）+ 测试集 ~200 条 + DPO ~1000 对
- 训练：主方案云上 Qwen3-8B LoRA；本地 Qwen3-0.6B 仅流程冒烟验证
- 评估：evalscope + DeepSeek LLM-as-a-Judge；基线对比（Prompt Engineering → SFT → DPO）
- 详细方案见 `微调案例实施方案.md`

### LOG-002 [里程碑] M3 种子清单完成 ✅
- 产出：`data/seeds/` 下 4 类种子清单各 25 条（共 100 条）+ `README.md`
- 校验：100 条全部通过 JSON 解析；类别 2 紧急度分布 高 9 / 中 10 / 低 6
- 安全红线：故障诊断 9 条高紧急度均含"立即停车/呼叫拖车/禁止行驶"；中/低条目均含升级条件

### LOG-003 [环境] DEEPSEEK_API_KEY 未设置
- 数据构造与 LLM-as-a-Judge 评估依赖 DeepSeek API，当前环境变量 `DEEPSEEK_API_KEY` 未设置
- 处理：运行生成前需配置（待用户提供或用户自行设置）

### LOG-004 [约定] 里程碑同步更新机制
- 约定：每完成一个里程碑，更新 `微调案例实施方案.md` 的里程碑表格状态（⬜→✅），并在本日志追加记录

### LOG-005 [异常] 类别4 小批生成失败与修复
- 现象：`build_data.py --mode small` 在类别 4 报 `KeyError: 'scenario'`（exit 1），前 3 类已成功
- 原因：类别 4 种子字段为 `complaint_type / customer_message / response_structure / key_points`，脚本误用 `scenario`
- 处理：修正 `build_prompt` 类别 4 为 `seed["complaint_type"]`，补跑 `--category 4` 成功（exit 0）
- 影响：前 3 类样本未受影响；已记录字段命名规范，后续脚本避免同类错误

### LOG-006 [进展] M4 小批样本生成完成（待验货）
- 产出：`data/generated/category{1..4}_*.jsonl` 各 10 条，共 40 条
- 质量自检：类别 1 政策事实正确；类别 2 高紧急度处理保守合规；类别 3 话术骨架完整、均"给出路"；类别 4 共情→担责→补救→升级完整、无过度承诺
- 待办：用户验货确认后进入 M5 批量构造

### LOG-007 [里程碑] M4 小批样本验货通过 ✅（2026-08-19）
- 用户确认 4 类样本质量 OK
- 确认数据规模口径：**生成总量 ~2000 条 = 训练集 ~1800 + 测试集 ~200**（每类 500 = 450 训练 + 50 测试，按种子分组 9:1 切分）

### LOG-008 [进展] M5 批量生成启动
- 命令：`build_data.py --mode full`（每类 25 种子 × 20 变体 ≈ 500 条/类，4 类 ≈ 2000 条）
- 后台运行中；完成后将执行校验 + 按种子分组切分 train/test + 生成评估集（evalscope general_qa 格式）

### LOG-009 [异常] M5 批量生成中断与修复
- 现象：full 模式运行至类别 2 种子 5 时崩溃（exit 1）
- 原因：LLM 偶发返回格式异常 JSON（数组元素为字符串），`validate_item` 未做类型容错，`item.get` 抛 AttributeError
- 处理：
  1. `validate_item` 增加 `isinstance(item, dict)` 检查，格式异常条目直接跳过（丢弃该条，不中断任务）
  2. `--category` 改为支持逗号分隔多值（如 `2,3,4`），便于断点续跑
- 影响与断点：类别 1 已完整生成 **493 条**（全部带 seed_idx 标记，文件完好，无需重跑）；类别 2/3/4 用修复后脚本补跑中
- 经验：LLM 批量生成必须对输出做类型与结构容错，单条异常跳过而非整体失败

### LOG-010 [优化] M5 生成速度优化
- 现象：full 模式每种子 4 次调用（per-call=5），4 类共约 400 次 API 调用，速度偏慢
- 处理：
  1. 中断当前任务，保留已完成的类别 1（493 条）与类别 2（465 条）
  2. `--per-call` 提高到 10，每种子调用次数减半（4 次 → 2 次），类别 3/4 重跑中
- 影响：类别 1/2 数据保留不重跑；预计剩余 2 类耗时减半

### LOG-011 [异常] per-call=10 触发大量连接中断，改用断点续跑
- 现象：`--per-call 10` 后出现大量 `IncompleteRead(0 bytes read)`（输出过长导致连接被切断），类别 3 仅 130 条（覆盖 9/25 种子）、类别 4 仅 1 条
- 根因分析：单次调用输出 token 过多 + DeepSeek API 连接不稳定，per-call=5 时（类别 1/2）成功率高，per-call=10 成功率骤降
- 处理（脚本增强）：
  1. 重试次数 3→5，超时 120s→180s，重试间隔加大
  2. 新增 `--resume` 断点续跑：读取已有文件，只补跑变体不足 20 条的种子，append 写入，不重写已有数据
  3. per-call 回退到 5
- 现状：类别 1=493 条 / 类别 2=465 条 / 类别 3=130 条 / 类别 4=1 条；类别 3、4 用 `--resume` 补跑中
- 经验：LLM 批量生成的"单次输出条数"不宜过大（≥10 会显著增加连接失败率），配断点续跑机制保证可恢复

### LOG-012 [暂停] M5 批量生成暂停（用户离开，网络不可用）
- 时间：2026-08-19，用户有事需离开且后续无网络
- 已停止后台任务；所有数据已落盘（断点续跑机制保证可随时恢复，已有数据不丢）
- **当前数据盘点**（data/generated/，共 1770 条）：

| 类别 | 条数 | 完整种子 | 缺的种子 |
|---|---|---|---|
| 1 知识问答 | 493 | 23/25 | 7, 10 |
| 2 故障诊断 | 465 | 23/25 | 2, 24 |
| 3 话术生成 | 482 | 21/25 | 3, 5, 12, 25 |
| 4 投诉处理 | 330 | 13/25 | 1, 3, 12, 15, 18~25 |

- **恢复方法（网络恢复后执行，一条命令继续补）**：
  ```
  python scripts/build_data.py --mode full --category 3,4 --resume
  python scripts/build_data.py --mode full --category 1,2 --resume
  ```
  （resume 模式只补变体不足 20 条的种子，已完整的自动跳过；可反复执行直到 4 类都齐）
- **补完后**：`python scripts/split_data.py`（按种子分组切分训练/测试 + 生成评估集）
- 切分脚本与方案文档均已在磁盘，无需网络即可运行

### LOG-013 [里程碑] M5 批量数据构造完成 ✅（2026-08-19）
- 生成总量 **2240 条**（每种子 20 条；投诉处理按用户要求加量到每种子 30 条）：
  | 类别 | 条数 | 种子覆盖 |
  |---|---|---|
  | 1 知识问答 | 500 | 25/25 |
  | 2 故障诊断 | 500 | 25/25 |
  | 3 话术生成 | 498 | 24/25（种子5 差 2 条，可忽略） |
  | 4 投诉处理 | 742 | 25/25 |
- 校验：全部 JSON 合法、seed_idx 标记齐全
- 切分（`scripts/split_data.py`，按种子分组 9:1，固定随机种子 42 可复现）：
  - 训练集 `data/sft_train.jsonl`：**2041 条**（450/450/449/692）
  - 测试集 `data/sft_test.jsonl`：**199 条**（50/50/49/50，含 category 标注）
  - 评估集 `data/eval/*.jsonl`：4 个文件共 199 条（general_qa 格式 {"query": ...}）
- 下一步：M6 SFT 训练（先本地 0.6B 冒烟验证流程，再云上 8B）

### LOG-014 [里程碑] M6 SFT 冒烟测试通过 ✅（本地 Qwen3-0.6B）
- 新增脚本：`scripts/train_sft.py`（LoRA SFT：r16/α32/all-linear，SFTConfig + assistant_only_loss + new_chat_template.jinja）、`scripts/merge_model.py`（adapter 合并）
- **[异常] `datasets.load_dataset` 在本机卡死**：进程 CPU 几乎不涨、日志停在数据加载阶段（Windows 沙箱下 datasets 4.x 的 hub/多进程行为）。修复：改用纯 Python 读 JSON + `Dataset.from_list` + 强制 `HF_HUB_OFFLINE/TRANSFORMERS_OFFLINE=1`，解决
- 冒烟结果（200 条 × 20 步）：loss 3.47→2.41，token 准确率 0.39→0.51；20 步仅 17.7s
- 合并验证：`output/sft_smoke_merged/`（完整模型 1.43GB）✅
- 推理链路验证：合并模型正常生成回复（冒烟仅验证流程，回复质量不做参考）✅
- 待办：正式训练（本地 0.6B 全量 2041 条 或 云上 8B）→ 合并正式 SFT 模型 → M7 评估

### LOG-015 [课题记录] epochs=3 是否过多 / 灾难性遗忘风险（待讨论，先不处理）
- 背景：全量 SFT 当前配置 `--epochs 3`（2041 条 × 3 = 192 步），用户提出 3 epochs 是否过多、是否引发灾难性遗忘
- 初步分析（供后续讨论）：
  - LoRA 只训练少量参数，灾难性遗忘风险显著低于全参微调
  - 2041 条属小数据量，2~3 epochs 是 LoRA SFT 的常见区间
  - 风险信号可观察：训练中 eval_loss 是否先降后升（过拟合/遗忘迹象）；评估阶段看模型在**通用能力**（非本案例场景）上的表现是否明显退化
- 决策：**先不处理**，当前训练继续（3 epochs）。待 M7 评估结果出来后，若发现过拟合或通用能力退化，再讨论对策（降为 1~2 epochs / early stopping / 增加数据多样性）
- 状态：待讨论 🔵

### LOG-016 [里程碑] M6 全量 SFT 训练完成 ✅（本地 Qwen3-0.6B，2026-08-20）
- 训练：2041 条 × 3 epochs = 192 步，耗时 12 分 28 秒（batch 4 × accum 8，lr 2e-4）
- 结果：train_loss 2.213；**eval_loss 2.144→1.959 全程持续下降，无过拟合拐点**（回应 LOG-015 的 epochs 担忧：3 epochs 当前数据下未出现过拟合）；eval token 准确率 0.558
- 产出：
  - LoRA adapter：`output/sft_model/`（38.5MB）
  - 合并模型：`output/sft_model_merged/`（1.43GB，可直接被 evalscope/transformers 加载）
- tensorboard 事件：`logs/sft/`（服务 http://127.0.0.1:6006）
- 下一步：M7 评估（Prompt Engineering 基线 vs SFT 模型，evalscope + DeepSeek 裁判）

### LOG-017 [异常] 模型冒烟测试暴露 0.6B 效果短板（安全分级错误）
- 用户已在 vLLM 启动 SFT 模型（`/home/m1881/github_proj/vLLM-test/sft_model_merged/`，端口 8000，served-model-name `Qwen3-0.6B-finetuned`），服务可达 ✅
- 实测三类 query：
  - 类别 4 投诉：共情→复检→若确认责任→400 热线，**骨架学习到位** ✅
  - 类别 1 首保：答"免费"但出现事实错误"400公里内免费"（训练数据为"6个月或5000公里"）⚠️
  - 类别 2 刹车软：判"中紧急度，可以继续行驶"——**安全红线错误**（应为高紧急度、禁止行驶）❌
- 诊断（本地 transformers 加载同一模型输出一致，排除 vLLM 配置问题）：
  - **训练数据本身正确**：`data/generated/category2_故障诊断.jsonl` 中刹车软样本（seed 1）回复均明确"最高紧急度/绝对不能再开/呼叫拖车" ✅
  - 根因判断：**0.6B 容量不足**——高/中紧急度的回复结构相似（都含"到店检查制动液、管路、总泵"），0.6B 无法精确区分分级；数字类知识（"6个月/5000公里"）记忆也差。符合方案文档预判的"0.6B 效果天花板"
- 对策（待用户决策，先记录）：① 继续评估如实呈现 0.6B 弱项；② 换云上 8B；③ 强化数据区分度（高紧急度用更强标记词、提高高紧急度样本占比）后重训
- 状态：待讨论 🔵

### LOG-018 [课题记录] 评估集优化：给裁判模型提供标准答案（reference）
- 用户提出（观点正确）：当前评估集 `data/eval/*.jsonl` 只有 `{"query": ...}`，LLM-as-a-Judge **无参考打分**，裁判只能凭"常识"判断信息是否准确；应提供标准答案让裁判对照打分，信息准确度才有客观依据
- 现状：`data/sft_test.jsonl` 中每条本就含标准回复（assistant 内容），评估集转换时丢掉了——**标准答案现成，可低成本恢复**
- 优化方案（待实施，评估跑完后处理）：
  1. 评估集每条增加标准答案字段（evalscope general_qa 支持 `target` 字段，review 文件中已见该字段）
  2. judge prompt 增加"标准答案"部分，裁判对照打分（占位符需验证，如 `{target}`）
  3. 评分标准中"信息准确"维度改为对照标准答案判断
- 影响：评估分数更客观可信；SFT/DPO 两轮评估统一使用（可复现对比）
- 状态：待实施 🔵（当前正在跑的评估为无参考版本，结果仅供参考，优化后需重跑）

### LOG-019 [进展] 评估集优化已实施（LOG-018 落地）+ 无参考评估结果
- **无参考版 SFT 评估结果（deepseek-v4-pro，2026-08-20，仅供参考）**：
  | 类别 | mean_acc |
  |---|---|
  | 1 知识问答 | 0.132 |
  | 2 故障诊断 | 0.180 |
  | 3 话术生成 | 0.234 |
  | 4 投诉处理 | 0.538 |
  | 总体 | 0.271 |
  - 样例佐证：样本"车买7个月1200公里首保能否免费"，模型误答"6个月应该符合可以免费"（实际已超期），裁判 0.35 合理
  - 结论：0.6B 对条件逻辑/数字记忆弱（知识问答分最低），骨架类（投诉处理）学得最好
- **优化实施**：
  1. 新增 `scripts/convert_eval.py`：从 sft_test.jsonl 提取标准回复，评估集升级为 `{"query", "answer"}`（evalscope general_qa 从 `answer` 字段读标准答案，judge 用 `{gold}` 占位符）
  2. `eval_runner.py`：judge prompt 增加[标准答案]对照段（信息准确维度对照 gold 打分）；**judge 模型改为 deepseek-v4-flash**（用户指定）
  3. 已重跑 SFT 评估（tag: sft_model_ref），完成后对比新旧打分
- 待办：SFT 重跑结果 → 基座基线（Prompt Engineering）评估 → 对比结论

### LOG-020 [进展] 基线（Prompt Engineering）评估启动
- 新增 `scripts/gen_baseline_eval.py`：基线评估集（199 条）= 系统提示词（角色/输出规范/安全红线/投诉骨架）+ query + 标准答案，输出 `benchmark/custom_eval/text/qa_baseline/`
- `eval_runner.py` 增加 `--local-path` 参数支持不同评估集目录
- 用户已把 vLLM 切换到基座模型 Qwen3-0.6B（served-model-name Qwen3-0.6B）
- **[重要观察]** 单样本（刹车软）：基座+系统提示词判断"紧急情况立即停车联系拖车"（正确）**优于** SFT 模型的"中紧急度可以继续行驶"（错误）——SFT 后 0.6B 在部分维度可能不如强 Prompt Engineering 基线，等全量结果验证
- 基线评估运行中（tag: baseline，同一评估集+同一 flash 裁判，与 SFT 结果可对比）

### LOG-021 [里程碑] M7 第一轮评估完成 ✅：SFT 模型 > Prompt Engineering 基线
- 评估配置：同一评估集（199 条，含标准答案）+ 同一裁判（deepseek-v4-flash + {gold} 对照）
- **对比结果（mean_acc，0~1）**：
  | 类别 | 基线（基座+Prompt） | SFT 模型 | 提升 |
  |---|---|---|---|
  | 1 知识问答 | 0.252 | 0.547 | +0.295 |
  | 2 故障诊断 | 0.396 | 0.556 | +0.160 |
  | 3 话术生成 | 0.176 | 0.494 | +0.318 |
  | 4 投诉处理 | 0.172 | 0.842 | +0.670 |
  | **总体** | **0.249** | **0.610** | **+0.361** |
- **结论：成功标准达成**——微调后的 SFT 模型在全部 4 类上均明显优于 Prompt Engineering 基线（总体 +0.36）
- 说明：此前单样本（刹车软）基座判断正确属个例，全量上基座 0.6B 输出不稳定（投诉/话术仅 0.17），SFT 后各维度显著稳定提升
- 结果落盘：`outputs/evalscope/baseline/20260820_214426/`、`outputs/evalscope/sft_model_ref/20260820_173951/`
- 下一步：M8 DPO 数据构造（偏好对 ~1000 对，方案第 5.3 节：阶段 A 改写型 70% + 阶段 B 模型排序型 30%）

### LOG-022 [进展] M8 DPO 数据构造启动（阶段 A 改写型）
- 新增 `scripts/build_dpo.py`：从 SFT 生成数据取 (user, assistant)，按类别缺陷维度让 LLM 改写 rejected
  - 缺陷维度：知识问答（漏关键条件/价格乱报/语气冷淡/不促行动）、故障诊断（紧急程度误判/建议不安全/无到店建议/信息缺失）、话术生成（不道歉/只拒绝不给方案/语气生硬/无促行动）、投诉处理（辩解推诿/无共情/无补救/无升级路径）
- **[异常]** 首跑 0 对：`build_data.call_llm` 强制 `response_format=json_object`，而改写 prompt 要求纯文本，DeepSeek 返回空。修复：`call_llm` 增加 `json_mode` 参数（默认 True，DPO 改写用 False）
- 小批验货（20 对）质量通过：缺陷明显、与 chosen 差异清晰；类别 2 反例（"让继续行驶"类危险建议）质量最佳
- full 模式启动（1000 对：200/200/300/300），运行中
- 待办：full 完成后 → 校验 → M9 DPO 训练（vLLM 需切回 SFT 模型做阶段 B 或直接开始 DPO 训练）

### LOG-023 [里程碑] M8 DPO 数据构造（阶段 A 改写型）完成 ✅
- 产出：`data/dpo_pairs.jsonl` **998 对**（知识问答 200 / 故障诊断 200 / 话术生成 300 / 投诉处理 300，差 2 对可忽略）
- 校验：字段完整 0 异常、chosen≠rejected 0 异常、rejected 长度 31~307（平均 125）
- 抽样确认质量：rejected 缺陷明显（只拒绝不给方案/辩解推诿/危险建议等）
- 说明：阶段 B（模型排序型 ~300 对）尚未做——依赖 SFT 模型推理；可选增强，用户可决定是否补充
- 下一步：M9 DPO 训练（train_dpo.py：trl DPOTrainer + LoRA，在 SFT 模型基础上继续训练）

### LOG-024 [进展] M9 DPO 训练启动（含兼容问题修复）
- 新增 `scripts/train_dpo.py`（trl DPOTrainer + LoRA，从 SFT 合并模型继续训练，beta 0.1 / lr 1e-5）
- **[异常]** DPOTrainer 初始化报 `AttributeError: 'Qwen3ForCausalLM' object has no attribute 'warnings_issued'`：trl 0.24 在 __init__ 访问 `model.warnings_issued`（transformers Trainer 属性），peft 0.18 包装后 __getattr__ 代理到 base model 找不到
  - 修复：模型加载后预置 `model.warnings_issued = {}` 实例属性（peft 代理可找到）
  - 另确认 DPOTrainer 支持 `peft_config` 参数（已用，由 trl 内部做 LoRA 包装）
- 冒烟通过（40 对 × 10 步，12.6s）：rewards/chosen 0.068 > rejected 0.024、logps/chosen -167.5 > rejected -230.9、accuracies 0.6——模型开始学会偏好区分
- 全量训练启动（998 对 × 2 epochs，预计几分钟）
- 待办：全量完成后合并 DPO 模型 → M10 第二轮评估（SFT vs DPO，需 vLLM 切回 SFT 模型）

### LOG-025 [里程碑] M9 DPO 训练完成 ✅
- 训练：998 对 × 2 epochs = 64 步，耗时 12 分 14 秒（beta 0.1，lr 1e-5，batch 4 × accum 8）
- 最终指标（偏好对齐效果显著）：
  - **rewards/chosen 0.60 vs rewards/rejected -2.17**（差距大）
  - **rewards/accuracies 0.9875**（几乎完美区分 chosen/rejected）
  - **rewards/margins 2.77**、loss 0.156
- 产出：
  - LoRA adapter：`output/dpo_model/`
  - 合并模型：`output/dpo_model_merged/`（1.43GB，基于 SFT 模型合并）
- 下一步：M10 第二轮评估（SFT vs DPO，同一评估集 qa + 同一 flash 裁判；需把 vLLM 切到 DPO 模型）

### LOG-026 [里程碑] M10 第二轮评估完成 ✅：DPO > SFT（偏好对齐有效）
- 评估配置：与 SFT 完全相同（同一评估集 qa 199 条 + 同一 flash 裁判）
- **三模型最终对比（mean_acc）**：
  | 类别 | 基线 | SFT | DPO | DPO vs SFT |
  |---|---|---|---|---|
  | 1 知识问答 | 0.252 | 0.547 | 0.563 | +0.017 |
  | 2 故障诊断 | 0.396 | 0.556 | 0.603 | +0.047 |
  | 3 话术生成 | 0.176 | 0.494 | 0.516 | +0.022 |
  | 4 投诉处理 | 0.172 | 0.842 | 0.841 | -0.001（持平） |
  | **总体** | **0.249** | **0.610** | **0.631** | **+0.021** |
- **两条成功标准全部达成**：
  1. SFT > 基线（+0.361）——微调有效
  2. DPO > SFT（+0.021，3 类提升 1 类持平无回退）——偏好对齐有效
- 观察：故障诊断 DPO 提升最明显（+0.047）；投诉处理持平高位（0.84）；刹车软单样本仍判"中紧急度"（0.6B 知识天花板，DPO 无法注入知识，符合 LOG-017 预判）
- 结果落盘：`outputs/evalscope/dpo_model/`
- 下一步：M11 总结报告（完整案例报告：数据/训练/评估全过程 + 三模型对比 + 典型案例）

### LOG-027 [进展] 8B 对比实验启动（云上 4090D，仅 SFT）
- 背景：用户要求先用 Qwen3-8B + 相同数据做 SFT 对比（验证 0.6B 的知识/安全分级短板是否因模型规模改善），总结报告推迟
- 云环境：AutoDL 4090D 24GB（`connect.westb.seetacloud.com:32305`，SSH 密钥免密已配置），Python 3.12 + torch 2.12.1 + transformers 5.2.0 + trl 0.24 + peft 0.20 + modelscope 1.34
- 模型：`/root/.cache/modelscope/hub/Qwen3-8B`（16GB 完整）
- 数据/脚本已上传 `/root/finetune_proj/`（train_sft.py 新增 `--qlora` 支持）
- 训练配置（与 0.6B 可比）：相同 2041 条 × 3 epochs，lr 2e-4，LoRA r16/α32，有效 batch 32（batch 2 × accum 16），**4bit QLoRA**（24GB 稳妥）
- 状态：训练运行中（PID 2068，预计 1.5~2.5 小时）
- 待办：训练完成 → 合并 8B 模型 → vLLM 起 8B → 同配置评估 → 与 0.6B 三模型对比

### LOG-028 [里程碑] 8B SFT 训练完成 ✅（云上 4090D，2026-08-21）
- 训练：相同 2041 条 × 3 epochs = 192 步，耗时 **22 分 31 秒**（4bit QLoRA，batch 2 × accum 16）
- 结果对比（同数据同配置）：
  | 指标 | 0.6B | 8B |
  |---|---|---|
  | eval_loss | 1.959 | **1.452** |
  | eval token accuracy | 0.558 | **0.633** |
  | train_loss | 2.213 | 1.640 |
- **[异常] 系统盘空间不足**：合并 8B 模型（16GB）写盘失败（`No space left on device`，系统盘 30G 仅剩 9G）→ 改为写入数据盘 `/root/autodl-tmp`（50G），成功
- 产出（云上）：adapter `output/sft_model_8b/`、合并模型 `/root/autodl-tmp/sft_model_8b_merged/`
- 待办：云上安装 vllm → 起 8B 推理服务 → 本地 SSH 隧道 → 同配置评估（带标准答案 + flash 裁判）→ 与 0.6B 对比

### LOG-029 [异常] 云上 vLLM 启动排障（8B）
- 现象与处理（依次）：
  1. `Engine core initialization failed` → 日志定位 **KV cache 不足**：8B bf16 占 16GB，max-model-len 32K 需 4.5GB KV cache > 可用 3.28GB → 降 `--max-model-len 8192`（评估数据 <2K token，足够）
  2. 仍失败 → **缺 ninja**（`FileNotFoundError: ninja`）：apt 无源、conda 装不上、pip ninja 纯 Python 无二进制 → **手动下载 GitHub ninja 1.12.1 静态二进制到 /usr/local/bin** ✅
  3. 最终：vLLM 0.27.1 成功启动（Qwen3-8B-sft，max-len 8192，gpu-mem 0.9）
- 注意：装 vllm 时 pip 把云上 transformers 升到 5.15.1、torch 升到 2.13.0（训练已完成不受影响；评估走 API 不受影响）
- 本地 SSH 隧道：`8001 -> 云上 8000`，8B 评估运行中（tag: sft_model_8b）

### LOG-030 [里程碑] 8B SFT 评估完成 ✅：8B 全面超越 0.6B
- 评估配置：与 0.6B 完全相同（同一评估集 qa 199 条 + 带标准答案 + flash 裁判）
- **四模型对比（mean_acc）**：
  | 类别 | 基线(0.6B+Prompt) | 0.6B SFT | 0.6B DPO | **8B SFT** |
  |---|---|---|---|---|
  | 1 知识问答 | 0.252 | 0.547 | 0.563 | **0.776** |
  | 2 故障诊断 | 0.396 | 0.556 | 0.603 | **0.727** |
  | 3 话术生成 | 0.176 | 0.494 | 0.516 | **0.700** |
  | 4 投诉处理 | 0.172 | 0.842 | 0.841 | **0.882** |
  | **总体** | **0.249** | **0.610** | **0.631** | **0.771** |
- **结论**：8B SFT 全面超越 0.6B（+0.16）；0.6B 短板（知识问答数字记忆 +0.23、故障诊断安全分级 +0.17）显著改善；8B 单轮 SFT 即超过 0.6B SFT+DPO 组合——验证模型规模是关键瓶颈（LOG-017 预判成立）
- 待办：① 8B 是否继续做 DPO（可选）② M11 总结报告（现在可纳入 8B 对比）

### LOG-031 [异常] 8B DPO 云上排障（mergekit/llm_blender/transformers 版本/显存）
- 项目已复制到数据盘 `/root/autodl-tmp/finetune_proj/`（50G，剩余 35G）
- 依次排障：
  1. `No module named 'mergekit'` → pip 装 mergekit 0.1.4
  2. `No module named 'llm_blender'` → pip 装 llm_blender → 反而报 `TRANSFORMERS_CACHE` 错 → **根因：trl 0.24 的 judges.py 对 llm_blender 是可选 import（try/except），不装才正常；装了反而触发其内部旧 API 错误** → 卸载 llm_blender ✅
  3. trl 0.24 与 transformers 5.12/5.15 不兼容 → 降回 transformers 5.2.0（与本地一致）
  4. **CUDA OOM**（8B vLLM 服务占 22.4GB 显存）→ 停掉 vLLM 释放显存 ✅
- 8B DPO 训练启动成功（998 对 × 2 epochs，QLoRA batch2×accum16，PID 13434）
- 注意：降 transformers 后 vllm 0.27.1 版本约束（>=5.5.3）不满足——8B DPO 评估前需处理（升回 transformers 或换评估方式）
- 待办：8B DPO 训练完成 → 合并 → 评估（与 0.6B DPO 对比）

### LOG-033 [事件] 云实例关机换新，8B SFT adapter 丢失（数据盘未保留）
- 用户决定关机并克隆新实例（仅保留系统盘）；数据盘 `/root/autodl-tmp` 内容不保留
- 尝试下载 8B SFT adapter（output/sft_model_8b，40MB）时实例已关机（Connection refused）——**adapter 丢失，需在新实例重跑 8B SFT（22 分钟）**
- 未受影响（本地完好）：全部数据、脚本、0.6B SFT/DPO 模型与评估结果、三轮评估数据
- 新实例恢复计划：确认环境（transformers 5.2/trl 0.24）→ 建 vllm 独立 conda 环境 → 上传数据脚本 → 重跑 8B SFT → 合并 → 8B DPO → 合并 → vllm 评估
- 经验：云上工作区（数据盘）关键产物应**先下载到本地再关机**；AutoDL 克隆仅保留系统盘

### LOG-034 [课题记录] evalscope 本地模型路径直评测试（待执行 🔵）
- 用户提出：评估时不通过 vLLM 部署，直接传**本地模型路径**给 evalscope，验证能否跑通 + 耗时多少
- 预期方式：`evalscope eval --model <本地模型路径> --model-args '{"dtype":"bfloat16","device_map":"auto"}' ...`（evalscope 内部用 transformers 加载推理，不走 --api-url）
- 本地可测：0.6B 模型（`model/Qwen3-0.6B`、`output/sft_model_merged`、`output/dpo_model_merged`）可直接本地测（16GB 显存够）；8B 需云上或本地 QLoRA
- 价值：省去 vLLM 部署步骤，评估更轻量；但需对比耗时与结果一致性
- 状态：待执行 🔵（等新实例/后续安排）

### LOG-035 [进展] LOG-034 实施：本地 0.6B SFT 模型直评测试
- 对象：本地 `output/sft_model_merged`（0.6B SFT），不通过 vLLM
- 方式：`eval_runner.py` 新增 `--local-model` 模式（evalscope --model 传本地路径 + --model-args，不传 --api-url）
- 结果目录：`outputs/evalscope/sft_model_local/`（**不覆盖** vLLM 评估的 `sft_model_ref`）
- 同一评估集（带 answer）+ 同一裁判（flash + {gold}），与 vLLM 方式可比
- 计时中；完成后对比：能否跑通 + 耗时 + 分数一致性（vs sft_model_ref 0.610）

### LOG-036 [里程碑] LOG-034 完成：evalscope 本地模型路径直评可行 ✅
- **[异常]** 首跑失败：本地模式 transformers generate 报 `temperature=0.0 has to be strictly positive`（vLLM 接受 0，transformers 不接受）→ 修复：本地模式 generation-config 用 `do_sample=False` 替代 temperature=0
- 结果（本地 `output/sft_model_merged`，199 条，同评估集同裁判）：
  | 类别 | vLLM 评估 | 本地直评 | 差异 |
  |---|---|---|---|
  | 1 知识问答 | 0.547 | 0.540 | -0.007 |
  | 2 故障诊断 | 0.556 | 0.548 | -0.008 |
  | 3 话术生成 | 0.494 | 0.484 | -0.010 |
  | 4 投诉处理 | 0.842 | 0.846 | +0.004 |
  | 总体 | 0.610 | 0.605 | -0.005 |
- **结论**：① 跑通 ✅（`--local-model` 模式，不依赖 vLLM）② 耗时 9 分钟（比 vLLM 方式约 15 分钟更快）③ 分数差异 <1%，一致性高
- 结果目录：`outputs/evalscope/sft_model_local/`（未覆盖 vLLM 结果）
- 后续可选项：8B 本地直评（QLoRA）验证同样可行

### LOG-037 [进展] 新实例就绪 + 8B SFT 重跑启动
- 新实例：`connect.westb.seetacloud.com:13332`（免密已配置），RTX 4090 24GB，数据盘 150G
- 环境**干净**：transformers 5.2.0 + trl 0.24.0（未被污染，无需处理版本问题）；8B 模型在系统盘（克隆保留）
- 已上传：数据（sft_train/sft_test/dpo_pairs）+ 脚本（train_sft/train_dpo/merge）+ 模板 → `/root/autodl-tmp/finetune_proj/`
- 8B SFT 重跑启动（screen 会话 sft8b，192 步 ≈ 25 分钟，QLoRA）
- 后续计划：合并 → 8B DPO（screen）→ 合并 → **评估改用本地直评模式**（LOG-036 验证可行，不再折腾 vLLM）

### LOG-038 [进展] 新实例 8B 全流程推进（SFT→DPO→评估）
- 8B SFT 重跑完成：eval_loss 1.451、token acc 0.635（与旧实例一致）→ 合并 `output/sft_model_8b_merged`
- 8B DPO 训练完成：64/64 步，accuracies 1.0、margins 1.57（偏好区分极佳）→ 合并 `output/dpo_model_8b_merged`
- 云上安装 evalscope 1.6.0；DeepSeek key 已安全写入 `data/.deepseek_key`（云上 Linux 无注册表）
- **[异常]** screen 环境 PATH 不含 miniconda → `evalscope` 命令找不到 → eval_runner 增加绝对路径探测（`/root/miniconda3/bin/evalscope`）
- 8B DPO 评估运行中（**本地直评模式**，不走 vLLM）：`--model output/dpo_model_8b_merged --local-model --tag dpo_model_8b`
- 注：SSH 偶发连接超时（实例负载高 load≈40+），重试可恢复

### LOG-039 [里程碑] 8B DPO 评估完成 ✅——五模型对比收官
- 评估方式：**本地直评模式**（云上 evalscope --local-model，不走 vLLM；与 vLLM 方式差异 <1%，LOG-036 已验证可比）
- **8B DPO 结果（mean_acc）**：知识问答 0.781 / 故障诊断 0.769 / 话术生成 0.713 / 投诉处理 0.892 / **总体 0.789**
- 8B DPO vs 8B SFT：+0.018（4 类全提升，故障诊断 +0.042 最明显）
- **五模型最终对比**：
  | 模型 | 总体 |
  |---|---|
  | 基线（0.6B+Prompt） | 0.249 |
  | 0.6B SFT | 0.610 |
  | 0.6B DPO | 0.631 |
  | 8B SFT | 0.771 |
  | **8B DPO** | **0.789** |
- 结论：SFT 有效（+0.36）、DPO 有效（0.6B +0.02 / 8B +0.02）、模型规模是关键（8B 相对 0.6B +0.16）
- 待办：M11 总结报告（数据/训练/评估全流程 + 五模型对比 + 经验教训）

### LOG-032 [异常] 8B DPO 进程被 ssh 会话断开牵连终止（setsid 修复）
- 现象：训练正常跑到 19/64 步（loss 0.65→0.58、accuracies 0.94）后进程消失、显存释放，日志无任何 Python 错误
- 根因：通过 `ssh 'nohup ... &'` 启动，ssh 会话断开后进程组被终止（AutoDL 环境 nohup 未完全脱离）
- 修复：改用 **`setsid nohup ... < /dev/null &`**（创建新会话，彻底脱离控制终端）✅
- 重启后进程稳定（PID 14174，session leader），每步 ~8s，64 步约 10 分钟
- 经验：云上通过 ssh 启动长任务必须用 setsid（或 tmux/screen），仅 nohup 不可靠
